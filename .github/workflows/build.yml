name: Build Android Kernel (Skip-Modules-Mode)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
          bc bison build-essential flex git curl \
          libssl-dev libelf-dev libncurses-dev \
          python3 unzip zip cpio

      - name: Clone Proton Clang
        run: |
          git clone --depth=1 https://github.com/kdrag0n/proton-clang clang

      - name: Clone AnyKernel3
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3

      - name: Build Kernel Image Only
        run: |
          export PATH=$PWD/clang/bin:$PATH
          export ARCH=arm64
          export SUBARCH=arm64
          
          # CRITICAL: This must match your working kernel exactly to use old modules
          # Based on your photo, we use the stock suffix
          CUSTOM_NAME="-qgki-gc8c87694a044"

          MAKE_FLAGS=(
            ARCH=arm64
            O=out
            CC=clang
            LLVM=1
            LLVM_IAS=1
            CROSS_COMPILE=aarch64-linux-gnu-
            CROSS_COMPILE_ARM32=arm-linux-gnueabi-
            KBUILD_MODPOST_WARN=1
            LOCALVERSION=$CUSTOM_NAME
          )

          mkdir -p out
          make "${MAKE_FLAGS[@]}" vendor/holi-qgki_defconfig

          # Fix Section Mismatch (Fatal Error Fix)
          ./scripts/config --file out/.config -e CONFIG_SECTION_MISMATCH_WARN_ONLY
          
          # Performance & Fast Charging
          ./scripts/config --file out/.config -e CPU_FREQ_GOV_PERFORMANCE
          ./scripts/config --file out/.config -e HZ_1000
          ./scripts/config --file out/.config -e CONFIG_OPLUS_CHG

          make "${MAKE_FLAGS[@]}" CONFIG_SECTION_MISMATCH_WARN_ONLY=y olddefconfig

          # Build Image ONLY (No 'all' or 'modules' target to save time)
          make -j$(nproc) "${MAKE_FLAGS[@]}" CONFIG_SECTION_MISMATCH_WARN_ONLY=y Image
          
      - name: Prepare AnyKernel3 (Image Only)
        run: |
          # 1. Standard A/B & Path Fixes
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' AnyKernel3/anykernel.sh
          sed -i 's/is_slot_device=0/is_slot_device=1/g' AnyKernel3/anykernel.sh
          sed -i 's|block=/dev/block/platform/omap/omap_hsmmc.0/by-name/boot;|block=/dev/block/by-name/boot;|g' AnyKernel3/anykernel.sh
          
          # 2. DISABLE MODULE PATCHING (Crucial to skip modules)
          sed -i 's/do.modules=1/do.modules=0/g' AnyKernel3/anykernel.sh

          # 3. Copy only the Kernel Image
          if [ -f out/arch/arm64/boot/Image ]; then
            cp out/arch/arm64/boot/Image AnyKernel3/
          else
            echo "Build failed!"
            exit 1
          fi

          cd AnyKernel3
          # Ensure no old modules are accidentally included
          rm -rf modules/
          zip -r9 ../Cyber-NoModules-Holi.zip *

      - name: Upload Flashable Zip
        uses: actions/upload-artifact@v4
        with:
          name: Cyber-Kernel-NoModules
          path: ./Cyber-NoModules-Holi.zip
          
