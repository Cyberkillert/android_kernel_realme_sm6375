name: Build Android Kernel (Cyber-Standalone-Master)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
          bc bison build-essential flex git curl \
          libssl-dev libelf-dev libncurses-dev \
          python3 unzip zip cpio

      - name: Clone Proton Clang
        run: |
          git clone --depth=1 https://github.com/kdrag0n/proton-clang clang

      - name: Clone AnyKernel3
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3

      - name: Force Common Tree Logic
        run: |
          # 1. Ensure the Root Kbuild links to the techpack (Just like LineageOS does)
          if ! grep -q "obj-y += techpack/" Kbuild; then
            echo "obj-y += techpack/" >> Kbuild
          fi

          # 2. Re-map the techpack/Kbuild to match your hardware screenshot
          echo "obj-y += stub/" > techpack/Kbuild
          echo "obj-y += audio/" >> techpack/Kbuild
          echo "obj-y += camera/" >> techpack/Kbuild
          echo "obj-y += display/" >> techpack/Kbuild
          echo "obj-y += video/" >> techpack/Kbuild
          echo "obj-y += datapa/" >> techpack/Kbuild
          echo "obj-y += datarmnet/" >> techpack/Kbuild
          echo "obj-y += datarmnet-ext/" >> techpack/Kbuild

          # 3. Inject Wi-Fi drivers (Crucial for SM6375/Holi)
          if [ ! -d "techpack/qcacld-3.0" ]; then
            git clone --depth=1 https://github.com/LineageOS/android_kernel_qcom_qcacld-3.0 techpack/qcacld-3.0
            echo "obj-y += qcacld-3.0/" >> techpack/Kbuild
          fi

      - name: Build Kernel 5.4 (Lineage-Holi-Final)
        run: |
          export PATH=$PWD/clang/bin:$PATH
          export ARCH=arm64
          export SUBARCH=arm64
          
          # This name helps the system modules recognize the 'family'
          CUSTOM_NAME="-lineageos-Cyber-Standalone"

          MAKE_FLAGS=(
            ARCH=arm64
            O=out
            CC=clang
            LLVM=1
            LLVM_IAS=1
            CROSS_COMPILE=aarch64-linux-gnu-
            CROSS_COMPILE_ARM32=arm-linux-gnueabi-
            KBUILD_MODPOST_WARN=1
            LOCALVERSION=$CUSTOM_NAME
          )

          mkdir -p out
          make "${MAKE_FLAGS[@]}" vendor/holi-qgki_defconfig

          # --- APPLY COMMON TREE SECURITY BYPASS ---
          ./scripts/config --file out/.config -d MODULE_SIG
          ./scripts/config --file out/.config -d MODVERSIONS
          ./scripts/config --file out/.config -e MODULE_FORCE_LOAD
          ./scripts/config --file out/.config -e CONFIG_SECTION_MISMATCH_WARN_ONLY
          
          # Tuning for Realme 9 5G (Holi)
          ./scripts/config --file out/.config -e CPU_FREQ_GOV_PERFORMANCE
          ./scripts/config --file out/.config -e HZ_1000
          ./scripts/config --file out/.config -e CONFIG_OPLUS_CHG

          make "${MAKE_FLAGS[@]}" CONFIG_SECTION_MISMATCH_WARN_ONLY=y olddefconfig
          make -j$(nproc) "${MAKE_FLAGS[@]}" CONFIG_SECTION_MISMATCH_WARN_ONLY=y all

      - name: Prepare AnyKernel3 (The 'Bridge' Injection)
        run: |
          # Configure for Realme A/B
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' AnyKernel3/anykernel.sh
          sed -i 's/is_slot_device=0/is_slot_device=1/g' AnyKernel3/anykernel.sh
          sed -i 's/do.modules=0/do.modules=1/g' AnyKernel3/anykernel.sh
          sed -i 's/do.systemless=1/do.systemless=0/g' AnyKernel3/anykernel.sh
          sed -i 's|block=/dev/block/platform/omap/omap_hsmmc.0/by-name/boot;|block=/dev/block/by-name/boot;|g' AnyKernel3/anykernel.sh

          cp out/arch/arm64/boot/Image AnyKernel3/
          
          # MATCH THE COMMON TREE DIRECTORY STRUCTURE
          # LineageOS ROMs for Holi check these exact paths
          mkdir -p AnyKernel3/modules/vendor/lib/modules/5.4-gki
          mkdir -p AnyKernel3/modules/vendor_dlkm/lib/modules

          # Collect all compiled drivers (Wi-Fi, Camera, Audio)
          find out -name "*.ko" -exec cp {} AnyKernel3/modules/vendor/lib/modules/5.4-gki/ \;
          find out -name "*.ko" -exec cp {} AnyKernel3/modules/vendor_dlkm/lib/modules/ \;
          
          # Strip symbols (Makes modules loadable on GKI kernels)
          find AnyKernel3/modules/ -name "*.ko" -exec aarch64-linux-gnu-strip --strip-debug {} \;

          cd AnyKernel3
          zip -r9 ../Cyber-Kernel-Standalone.zip *

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Cyber-Kernel-Final
          path: ./Cyber-Kernel-Standalone.zip
          
